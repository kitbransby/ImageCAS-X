"""nnU-Net, external-CLI-only: this repo preps its data and reads its predictions
back, with no BaseLumenModel subclass.

    model.py       notes on the external pipeline
    data_prep.py   offline: convert this benchmark's layout to nnU-Net's
"""
